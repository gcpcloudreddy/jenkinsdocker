version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: test-circleci/test-circleci
    steps:
      - run: echo "Hi I'm on Runners!"
      - run: docker ps
      - run: cat /tmp/checksums.txt
      - run: cd /opt/scancli/fopnodeweb; python3.8 /opt/scancli/ScanCLI/cli/main.py -t oss
