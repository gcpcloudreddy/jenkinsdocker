version: 2.1
jobs:
  build:
    docker:
      - image: dockerhub.paypalcorp.com/docker-local/decisionengineservice:latest
    resource_class: test-circleci/test-circleci
    steps:
      - run: echo "Hi I'm on a Container Runner!"
workflows:
  build-workflow:
    jobs:
      - build
